.top-main
  .search-area
    %h2.search-area__title
      %span エリアから探す
    .search-area__area
      .selected-area
        .selected-area__name
          %span 東京
        .selected-area__store-count
          %span= "#{@shops.count}件"
      %ul.top-areas
        - @top_areas.each do |top_area|
          %li.top-area
            = link_to(shops_searches_path(area_id: top_area.id), class: 'top-area-link') do
              .top-area-box
                %span.top-area-name= top_area.name
                %span.top-area-shop-count= "#{top_area.shops.count}件"
      %ul.area-list.clearfix
        - @areas.each do |area|
          %li.all-area
            = link_to(shops_searches_path(area_id: area.id), class: 'area-link') do
              %span= area.name
  .search-genre
    %h2.search-genre__title
      %span ジャンルから探す
    .search-genre__area
      %ul.top-genres
        - @top_genres.each do |top_genre|
          %li.top-genre
            = link_to(shops_searches_path(genre_id: top_genre.id), class: 'top-genre-link') do
              = image_tag "/#{top_genre.image}", class: 'top-genre__img'
              .top-genre__name
                %span= top_genre.name
              .top-genre__store-count
                %span= "#{top_genre.shops.count}件"
      %ul.genre-list.clearfix
        - @genres.each do |genre|
          %li.all-genre
            = link_to(shops_searches_path(genre_id: genre.id), class: 'genre-link') do
              %span= genre.name
    .search-genre__all
      = link_to 'ジャンル一覧へ', genres_path, class: 'genre-all-link'
  .search-brand
    %h2.search-brand__title ブランドから探す
    .search-brand__area
      %ul.top-brands
        %li.top-brand
          %img{src: "/assets/brand-yohjiyamamoto.jpg", class: "top-brand__img", alt: "brand image1"}
        %li.top-brand
          %img{src: "/assets/brand-maisonmargiela.png", class: "top-brand__img", alt: "brand image1"}
        %li.top-brand
          %img{src: "/assets/brand-isseymiyakemen.jpg", class: "top-brand__img", alt: "brand image1"}
      %ul.brand-list.clearfix
        %li.all-brand
          Acne STUDIOUS
        %li.all-brand
          UNDERCOVER
        %li.all-brand
          MAISON KITSUNE
        %li.all-brand
          MARNI
        %li.all-brand
          HOMME PLISSÉ ISSEY MIYAKE
        %li.all-brand
          COMOLI
        %li.all-brand
          AURALEE
        %li.all-brand
          SUN SEA
        %li.all-brand
          UNUSED
        %li.all-brand
          Hender Scheme
        %li.all-brand
          KENZO
        %li.all-brand
          BALENCIAGA
        %li.all-brand
          MARGARET HOWELL
        %li.all-brand
          COMME des GARCONS
        %li.all-brand
          Raf Simons
        %li.all-brand
          lucien pellat-finet
        %li.all-brand
          ANN DEMEULEMEESTER
        %li.all-brand
          Y-3
    .search-brand__all
      = link_to 'ブランド一覧へ', brands_path, class: 'brand-all-link'

