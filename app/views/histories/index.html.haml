- breadcrumb :user_histories, @user
= render "layouts/breadcrumbs"
.user-wrap
  = render 'users/header'
- if @histories.count != 0
  = render 'histories', histories: @histories
- else
  .no-history
    %span 行ったお店は登録されていません。